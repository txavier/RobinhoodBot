# AGENTS for RobinhoodBot

## Git Policy

- **NEVER push to the git repository automatically.** The user controls when pushes happen. Always leave `git push` and `git commit` for the user to do manually.
