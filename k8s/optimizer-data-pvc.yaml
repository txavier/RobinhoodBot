apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: optimizer-data
  namespace: robinhoodbot
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
